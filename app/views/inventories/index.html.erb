<h1>Listado de Inventarios</h1>
<p>A continuación se listan los Inventarios almacenados en el sistema</p>
<br>

<div class="panel panel-default">
  <div class="panel-heading panel-heading-toolbox">
    <div class="panel-title">
      Listado de Inventarios
    </div>
  </div>
  <div class="panel-body">
    
    <table class="table table-hover">
      <thead>
        <tr>
            <th>Date</th>
            <th>Store</th>
            <th>User</th>
        </tr>
      </thead>

      <tbody>
        <% @inventories.each do |inventory| %>
          <tr>
              <td>
                <%= link_to inventory_path(inventory) do %>
                  <%= inventory.created_at %>
                <% end %>

              </td>
              <td><%= inventory.store %></td>
              <td><%= inventory.user %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <%= will_paginate @inventories, renderer: BootstrapPagination::Rails %>

  </div>
</div>

<br>
