<h1>Listado de impuestos</h1>
<p>A continuación se muestran los impuestos almacenados en el sistema.</p>
<br>

<div class="panel panel-default">
  <div class="panel-heading panel-heading-toolbox">
    <div class="panel-title">
      Listado de impuestos
    </div>
    <div class="panel-toolbar">
      <%= link_to new_tax_path, :class => "btn btn-success" do %>
        <i class="fa fa-plus"></i>
      <% end %>
    </div>
  </div>
  <div class="panel-body">
    
    <table class="table table-hover">
      <thead>
        <tr>
            <th>Nombre</th>
            <th>Valor</th>
        </tr>
      </thead>

      <tbody>
        <% @taxes.each do |tax| %>
        <tr>
          <td>
            <%= link_to tax do %>
              <%= tax.name %>
            <% end %>
          </td>
          <td><%= tax.value %> %</td>
        </tr>
        <% end %>
      </tbody>
    </table>

    <%= will_paginate @taxes, renderer: BootstrapPagination::Rails %>

  </div>
</div>

<br>
