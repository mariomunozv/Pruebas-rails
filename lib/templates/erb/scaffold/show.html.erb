<h1>Detalle del ###</h1>
<p>A continuación se muestra la información almacenada del ###.</p>
<br>

<div class="panel panel-default">
	<div class="panel-heading panel-heading-toolbox">
		<div class="panel-title">Detalle del ###</div>
		<div class="panel-toolbar">
			<%%= link_to 'Volver', <%= index_helper %>_path, :class => "btn btn-link" %>
			<%%= link_to edit_<%= singular_table_name %>_path(@<%= singular_table_name %>), :class => "btn btn-default" do %>
				<i class="fa fa-pencil"></i>
			<%% end %>
			<%%= link_to @<%= singular_table_name %>, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" do %>
				<i class="fa fa-trash-o"></i>
			<%% end %>
		</div>
	</div>
	<div class="panel-body">
		
		<table class="table table-bordered">
			<tbody>
			<% attributes.reject(&:password_digest?).each do |attribute| -%>
			<%- if attribute.reference? -%>
				<tr>
					<th><%= attribute.human_name %>:</th>
					<td><%%= @<%= singular_table_name %>.<%= attribute.name %>.name %></td>
				</tr>
			<%- else -%>
				<tr>
				  <th><%= attribute.human_name %>:</th>
				  <td><%%= @<%= singular_table_name %>.<%= attribute.name %> %></td>
				</tr>
			<%- end -%>
			<% end -%>
			</tbody>
		</table>

	</div>
</div>
